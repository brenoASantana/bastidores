# Quick Reference - BACKROOMS Dev\n\n## Inicializar Projeto\n\n```bash\nnpm install          # Instalar deps\nnpm run dev          # Dev server (http://localhost:3000 ou 3001)\nmake help            # Ver comandos Make\n```\n\n## Arquivos Mais Consultados\n\n| Arquivo | Propósito | Quando Editar |\n|---------|------------|---------------|\n| `src/config/constants.ts` | Configurações de jogo | Ajustar dificuldade, velocidade, volumes |\n| `src/store/gameStore.ts` | State global | Adicionar novo estado |\n| `src/systems/horrorSystem.ts` | Ansiedade & eventos | Novos eventos de horror |\n| `src/systems/audioSystem.ts` | Áudio & mixer | Mudar trilhas, volumes |\n| `src/components/game/PlayerController.tsx` | Input & physics | Controles, movimento, colisão |\n| `src/components/game/MapGeometry.tsx` | Mapa 3D | Adicionar salas/paredes |\n\n## Snippets Úteis\n\n### Acessar State Global\n\n```typescript\nconst store = useGameStore()\nconst anxiety = store.gameState.anxiety\nconst position = store.player.position\n\nstore.updateAnxiety(5)\nstore.setGameState('playing')\n```\n\n### Pegar Sistemas\n\n```typescript\nconst horror = horrorSystem\nconst audio = getAudioSystem()\nconst objective = objectiveSystem\n\naudio.playSFX(HORROR_EVENTS.WHISPER, 0.5)\n```\n\n### Game Loop (useFrame)\n\n```typescript\nuseFrame((state, delta) => {\n  // delta = tempo desde frame anterior (segundos)\n  // state.camera = câmera Three.js\n  // state.scene = cena\n  \n  const audioSystem = getAudioSystem()\n  const terror = horrorSystem\n  \n  // Sua lógica aqui (executa 60x/s)\n})\n```\n\n### Imprimir Debug\n\n```typescript\nconsole.log('State:', useGameStore.getState())\nconsole.log('Position:', useGameStore.getState().player.position)\nconsole.log('Anxiety:', useGameStore.getState().gameState.anxiety)\n```\n\n## Alterações Comuns\n\n### Mudar Velocidade do Jogador\n\n```typescript\n// src/config/constants.ts\nexport const PLAYER_CONFIG = {\n  MOVE_SPEED: 10,  // era 8\n}\n```\n\n### Mudar Taxa de Ansiedade\n\n```typescript\n// src/config/constants.ts\nexport const GAME_CONFIG = {\n  ANXIETY_RISE_RATE: 0.7,    // era 0.5 (mais rápido)\n  ANXIETY_FALL_RATE: 0.1,    // era 0.15 (cai mais lento)\n}\n```\n\n### Adicionar Novo Evento\n\n```typescript\n// 1. Declare em constants.ts\nexport const HORROR_EVENTS = {\n  NEW_EVENT: 'new_event',\n}\n\n// 2. Implemente em horrorSystem.ts\nshouldTriggerEvent(eventId: string, ...) {\n  if (eventId === HORROR_EVENTS.NEW_EVENT) {\n    return probability > Math.random()\n  }\n}\n\n// 3. Configure áudio em audioSystem.ts\nthis.addSFX(HORROR_EVENTS.NEW_EVENT, 'path')\n\n// 4. Dispare em PlayerController.tsx\nif (horrorSystem.shouldTriggerEvent(...)) {\n  audio.playSFX(HORROR_EVENTS.NEW_EVENT)\n}\n```\n\n### Adicionar Parede ao Mapa\n\n```typescript\n// src/components/game/MapGeometry.tsx\n<mesh position={[x, y, z]} castShadow>\n  <boxGeometry args={[width, height, depth]} />\n  <meshStandardMaterial color=\"#c0b0a0\" roughness={0.85} />\n</mesh>\n```\n\n### Aumentar Vinheta em Ansiedade Alta\n\n```typescript\n// src/systems/horrorSystem.ts\ngetVisualEffects(anxiety: number) {\n  const normalized = anxiety / MAX_ANXIETY\n  return {\n    vignette: Math.min(0.5, normalized * 0.6),  // aumentado de 0.5\n  }\n}\n```\n\n## Keyboard Shortcuts (VS Code)\n\n| Atalho | Ação |\n|--------|------|\n| `Cmd/Ctrl + P` | Abrir arquivo rápido |\n| `Cmd/Ctrl + F` | Buscar no arquivo |\n| `Cmd/Ctrl + Shift + F` | Buscar no projeto |\n| `Cmd/Ctrl + D` | Selecionar palavra / próxima ocorrência |\n| `Cmd/Ctrl + L` | Selecionar linha |\n| `Cmd/Ctrl + /` | Toggle comentário |\n\n## Debugging Rápido\n\n```typescript\n// Pausar em determinado ponto\ndebugger;\n\n// Log condicional\nif (anxiety > 90) console.log('CRÍTICO:', anxiety)\n\n// Measure performance\nconsole.time('label');\n// ... código ...\nconsole.timeEnd('label');\n```\n\n## Build & Deploy\n\n```bash\n# Build para produção\nnpm run build\n\n# Testar prod build local\nnpm run start\n\n# Limpar cache\nmake clean\n\n# Deploy via Vercel\nvercel --prod\n```\n\n## Estrutura de Cores (Tailwind)\n\nTemas do projeto:\n\n```\n- Fundo: #000000 (preto)\n- Paredes: #c0b0a0 (bege arenoso)\n- UI: #ffffff (branco) / #ff8800 (laranja)\n- Danger: #ff0000 (vermelho)\n- Safe: #00ff00 (verde)\n```\n\n## Responsibilidades por Arquivo\n\n### `gameStore.ts`\n- Estado global (anxiety, position, state)\n- Ações de atualização\n- **NÃO**: Lógica complexa\n\n### `horrorSystem.ts`\n- Calcular ansiedade\n- Efeitos visuais\n- Gatilhos de eventos\n- **NÃO**: Gerenciamento de state\n\n### `audioSystem.ts`\n- Reproduzir áudio\n- Mixer reativo\n- **NÃO**: Lógica de gameplay\n\n### `PlayerController.tsx`\n- Capturar input\n- Calcular movimento\n- Colisão\n- **NÃO**: Renderização 3D (exceto câmera)\n\n### `GameScene.tsx`\n- Iluminação\n- Geometria principal\n- **NÃO**: Lógica de gameplay\n\n## Variáveis Globais Úteis\n\n```typescript\n// Role your own quando necessário\nconst store = useGameStore()\nconst anxiety = store.gameState.anxiety\nconst playerPos = store.player.position\nconst horrorData = horrorSystem\nconst audioData = getAudioSystem().getState()\n```\n\n## Padrão de Nova Feature\n\n1. **Declare tipo/constante** em `config/constants.ts` ou `types/game.ts`\n2. **Implemente lógica** em sistema relevante (`horrorSystem.ts`, etc)\n3. **Integre no state** atualizando `gameStore.ts` se necessário\n4. **Dispatch em engine** no `PlayerController.tsx` ou componente apropriado\n5. **Render visual** em componente 3D (`MapGeometry.tsx`, etc)\n6. **Teste e documente**\n\n## Dúvidas Rápidas\n\n**P: Como alterar sensibilidade de mouse?**\nR: `PLAYER_CONFIG.MOUSE_SENSITIVITY` em `constants.ts`\n\n**P: Como aumentar/reduzir ansiedade manualmente?**\nR: `store.updateAnxiety(+5)` ou `store.updateAnxiety(-5)`\n\n**P: Como debugar colisão?**\nR: Adicione `console.log(playerPos)` em `PlayerController.tsx`\n\n**P: Como testar novo evento de horror?**\nR: Define probabilidade a 1 (`shouldTriggerEvent` retorna `true`)\n\n---\n\n**Precisa de ajuda?** Veja DEVELOPMENT.md (mais detalhado) ou abra issue.\n